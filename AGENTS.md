## 项目概述

`star-doc` 是基于 VitePress 的个人技术文档站，用于沉淀前端、浏览器、计算机基础、数据结构、网络、Linux 等方向的开发经验和底层原理。

- 文档正文、注释和面向读者的说明默认使用**中文**。
- 读写文件时注意 UTF-8 编码，避免中文乱码。
- 写作目标是准确、清晰、可复习：先解释概念边界，再说明机制，再给出例子和实践意义。
- 不把当前业务项目的 React/Zustand/API 规则套用到本项目；本项目的核心资产是 Markdown 文档、VitePress 配置和静态图片。

## 第一性原理

请从读者真实要解决的问题出发，不要只堆砌结论：

- 先明确文章要回答什么问题，以及读者需要具备哪些上下文。
- 对概念、机制和流程类内容，优先从定义、前提、运行过程、边界条件、常见误区展开。
- 对命令、配置和实践类内容，优先说明适用环境、执行步骤、验证方式和风险。
- 如果用户需求只说“整理一下”“补充一下”，先阅读同目录现有文章风格，再按最小必要范围修改。

## 强制使用的 Skills

以下场景必须先读取对应 Skill，再进行修改：

| 场景 | 必须使用的 Skill |
| --- | --- |
| 新增、重写、扩写技术文章，或调整文章结构与表达 | `/star-doc-writing` |
| 新增、移动、删除文档，或修改 `config/sidebar.ts` / `config/nav.ts` / 目录归类 | `/star-doc-navigation` |
| 文档修改后的链接、图片、代码块、VitePress 语法和轻量校验 | `/star-doc-verification` |

如一次任务同时涉及文章内容和导航配置，先使用 `/star-doc-writing` 明确正文改动，再使用 `/star-doc-navigation` 同步入口，最后使用 `/star-doc-verification` 自查。

## 目录结构

```text
star-doc/
├── .vitepress/      # VitePress 主题与站点内部配置
├── config/          # VitePress 拆分配置，如导航、侧边栏、搜索、语言、开发服务器
├── docs/            # 技术文章主体
├── public/images/   # 文档图片资源，文章中以 /images/xxx 引用
├── index.md         # 首页
├── package.json     # 脚本与依赖
└── AGENTS.md        # Agent 项目约束
```

## 文档写作规范

- 每篇文章必须有清晰的一级标题，标题直接描述主题，不使用空泛标题。
- 文章结构优先采用“概念定义 -> 核心机制 -> 流程/示例 -> 边界和误区 -> 实践意义”。不适合该结构时，以同目录既有文章风格为准。
- 技术术语首次出现时，尽量给出中文解释；常见英文缩写可保留英文并补充中文含义。
- 代码块必须带语言标识，例如 `js`、`ts`、`bash`、`http`、`text`。
- 示例代码应服务于解释目标，避免引入与主题无关的框架或业务背景。
- 对容易误解的内容，优先使用 VitePress callout，例如 `[!NOTE]`、`[!WARNING]`、`[!TIP]`。
- 引用外部资料时，优先使用官方文档或规范来源；不确定的事实必须标记为需要核验，不能写成确定结论。
- 不为了凑长度扩写。新增内容必须能提升准确性、可读性或复习价值。

## 导航与资源规范

- 新增文章后，必须评估是否同步 `config/sidebar.ts`；若文章属于顶层频道入口，也要评估 `config/nav.ts`。
- `docs/` 下的路径要和主题分类一致：前端放 `docs/frontend/`，网络放 `docs/network/`，Linux 放 `docs/linux/`，数据结构放 `docs/dataStructures/`，计算机基础放 `docs/computerScience/`。
- 图片放在 `public/images/`，Markdown 中使用 `/images/文件名` 引用。
- 图片文件命名使用英文、数字和连字符，避免中文文件名和空格。
- 移动或重命名文章时，必须同步更新所有相对链接、sidebar/nav 链接和相关文章引用。

## 修改边界

- 默认只修改与用户请求直接相关的文章、图片和配置。
- 不自动新增说明文档、迁移计划、临时总结等额外文件；确需临时文件时放在 `.agent/` 目录。
- 不自动提交 Git。
- 禁止 install/build/dev 任务，除非用户明确要求。
- 破坏性变更前必须征求确认，包括删除文章、批量重命名、重写大段历史内容、删除图片资源、修改核心 VitePress 配置。

## 验证要求

- 文档内容改动后，至少检查 Markdown 标题层级、链接路径、图片路径和代码块语言。
- 修改 `config/*.ts` 后，至少检查 TypeScript/JavaScript 语法和导出形式是否保持现有风格。
- 用户明确要求不运行校验命令时，只做静态阅读和 targeted search，并在交付说明中说明未运行命令。
